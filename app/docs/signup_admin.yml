Admin Signup.
---
tags:
 - User Authentication
parameters:
  - in: body
    name: body
    required: true
    type: string
    description: This route is used to register a new user. 
responses:
  201:
    description: 
    schema:
      id: register
      properties:
        email:
          type: string
          default: john.doe@example.com
        name:
          type: string 
          default: john doe
        password:
          type: string
          default: king
        username:
          type: string
          default: false